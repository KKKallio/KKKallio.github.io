import type { ReactNode } from 'react';
import '@/styles/page/resume.css';
import '@/styles/page/publications.css';

export default function PageLayout({ children }: { children: ReactNode }) {
  return children;
}
